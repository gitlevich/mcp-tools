[project]
name = "rename-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "rope>=1.13.0",
    "mcp>=1.2.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.setuptools]
py-modules = ["config", "rename_python", "rename_ts", "server"]
